[workspace]
members = [
    "client",
    "server",
    "shared",
]
resolver = "2"

[workspace.dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1.0.228", features = ["derive"] }
